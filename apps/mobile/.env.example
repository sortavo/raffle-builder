# Sortavo Mobile App Environment Variables
# Copy this file to .env.local and fill in your values

# Supabase Configuration (Required)
# Get these from your Supabase project settings
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# EAS Build Secrets (Set via `eas secret:create`)
# These are referenced in eas.json with @ prefix
# Run: eas secret:create --name supabase-url --value "https://your-project.supabase.co" --scope project
# Run: eas secret:create --name supabase-anon-key --value "your-anon-key" --scope project
# Run: eas secret:create --name apple-id --value "your@email.com" --scope project
# Run: eas secret:create --name asc-app-id --value "your-app-id" --scope project
